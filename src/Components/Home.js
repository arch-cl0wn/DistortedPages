import React from 'react';
import SwipeableTextMobileStepper from './carousel';

const Test = () => {
    return(
        <div>
            <SwipeableTextMobileStepper />
        </div>
    );
}

export default Test;
